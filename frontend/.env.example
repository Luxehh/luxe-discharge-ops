# Backend API base URL (no trailing slash)
# Local:
VITE_API_URL=http://localhost:5000
# Live (Railway API) — use this when building/deploying the frontend:
# VITE_API_URL=https://luxe-discharge-ops-server-production.up.railway.app
